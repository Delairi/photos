generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Folders {
  id         Int      @id @default(autoincrement())
  name       String
  created_at DateTime @default(now())
  username   String
}